# ── API (Flask) ──────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors
flask-jwt-extended
werkzeug>=3.0.0
gunicorn

# ── Database ─────────────────────────────────────────────────────────────────
psycopg[binary]>=3.1        # psycopg3 — used by db/database.py
psycopg2-binary             # psycopg2 — used by api/auth_routes.py

# ── Crawler / worker ─────────────────────────────────────────────────────────
playwright
boto3
httpx
beautifulsoup4
lxml
redis
greenlet>=3.0.3

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv
